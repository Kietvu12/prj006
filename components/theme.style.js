import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    backgroundContainer: {
        flex: 1,
        flexDirection: "row",
        height: "100%"
    },
})
export default styles
